# AGENTS.md

이 문서는 이 리포지토리에서 작업하는 AI 코딩 에이전트가 따라야 할 기본
지침입니다.

## 프로젝트 개요

이 프로젝트는 NEIS 공개 API를 활용해 초중고 급식 메뉴를 조회하고 분석하는
웹 애플리케이션을 단계별로 구현하는 워크숍입니다. 현재 리포지토리는 워크숍
가이드와 원본 데이터를 제공하며, 프론트엔드, 백엔드, 배포 구성은 워크숍을
진행하면서 추가됩니다.

## 일반 작업 지침

- 요청된 변경과 직접 관련된 범위만 수정하고 기존 동작을 보존합니다.
- 작업 전 관련 코드, 문서, 설정을 확인하며 더 구체적인 기존 규칙과 관례를
  이 문서보다 우선합니다.
- 현재 구조와 패턴을 따르고, 재사용할 수 있는 구현이 있는지 먼저 확인합니다.
- 필요한 수준보다 복잡한 설계, 이른 일반화, 불필요한 추상화나 의존성을
  추가하지 않습니다.
- 생성된 파일이나 외부 코드를 직접 수정하기 전에 원본 또는 생성 절차를
  확인합니다.
- 요구사항이 모호하거나 기존 동작과 충돌하면 임의로 결정하지 말고 확인합니다.

## Python 지침

- 공개 함수와 데이터 구조에 명확한 타입 힌트를 사용하고 타입 안정성을
  유지합니다.
- 예외를 무시하거나 광범위하게 처리하지 않습니다. 복구 가능한 오류만 구체적인
  예외로 처리하고, 사용자 또는 호출자가 대응할 수 있는 정보를 제공합니다.
- 입력과 외부 API 응답은 신뢰하지 말고 경계에서 검증합니다.
- 프로젝트에 구성된 포매터, 린터, 타입 검사기와 테스트 관례를 따릅니다.
- 새 패키지는 표준 라이브러리나 기존 의존성으로 해결할 수 없는 경우에만
  추가하고, 해당 의존성 선언과 잠금 파일을 함께 갱신합니다.

## TypeScript 지침

- `strict` 타입 검사를 전제로 명시적인 타입과 타입 좁히기를 사용합니다.
- `any`, 불필요한 타입 단언, null 가능성을 숨기는 코드를 피합니다.
- 외부 입력과 API 응답은 런타임에 검증하고 실패 상태를 명시적으로 처리합니다.
- 오류를 삼키거나 성공처럼 보이는 기본값으로 대체하지 않습니다.
- 프로젝트에 구성된 포매터, 린터, 타입 검사기, 빌드 도구와 컴포넌트 관례를
  따릅니다.
- 새 패키지는 기존 코드나 플랫폼 기능으로 해결할 수 없는 경우에만 추가하고,
  해당 의존성 선언과 잠금 파일을 함께 갱신합니다.

## 테스트 및 유효성 검사

- 동작을 변경하면 정상 경로와 주요 실패 및 경계 조건을 다루는 테스트를
  추가하거나 수정합니다.
- 변경 범위를 검증하는 가장 작은 기존 테스트, 포맷팅, 린트, 타입 검사 및
  빌드 절차를 실행합니다.
- 리포지토리에 아직 구성되지 않은 도구, 스크립트 또는 실행 명령을 임의로
  가정하거나 추가하지 않습니다.
- 검사를 실행할 수 없거나 기존 실패가 있으면 이를 숨기지 말고 변경 결과에
  명확히 기록합니다.

## 보안

- 비밀, 자격 증명, 토큰, 개인 정보 또는 실제 환경 설정 값을 코드, 문서,
  로그와 테스트 데이터에 포함하지 않습니다.
- 민감한 설정은 환경 변수나 프로젝트에서 정한 비밀 관리 방식을 사용합니다.
- 입력 검증, 권한 확인, 최소 권한 원칙을 적용하고 의존성 추가 시 출처와
  보안 영향을 검토합니다.
- 보안 취약점은 공개 이슈로 노출하지 않고 `SECURITY.md`의 절차를 따릅니다.

## 문서화

- 사용자 동작, 공개 API, 설정, 개발 절차가 바뀌면 관련 문서를 함께
  갱신합니다.
- 문서는 실제 구현과 일치해야 하며 확인되지 않은 명령이나 기능을 작성하지
  않습니다.
- 코드는 가능한 한 스스로 설명되도록 작성하고, 주석은 의도나 제약을 설명할
  때만 사용합니다.

## 주의사항 및 가드레일

- 사용자 변경이나 관련 없는 변경을 덮어쓰거나 되돌리지 않습니다.
- 명시적인 요청 없이 호환성을 깨는 변경, 데이터 삭제, 대규모 재구성 또는
  파괴적인 명령을 수행하지 않습니다.
- 테스트 통과를 위해 검증을 약화하거나 오류를 숨기지 않습니다.
- 라이선스와 저작권을 준수하며 출처가 불명확한 코드를 복사하지 않습니다.

## Git 커밋 및 Pull Request

- 커밋은 하나의 논리적 변경에 집중하고 Conventional Commits 형식의 메시지를
  사용합니다.
- 관련 없는 파일, 생성물, 비밀 또는 민감한 데이터를 커밋하지 않습니다.
- Pull Request는 `.github/PULL_REQUEST_TEMPLATE.md`를 반드시 사용하고 모든
  관련 항목을 정확히 작성합니다.
- Pull Request 설명에는 변경 이유, 관련 이슈, 변경 유형, 수행한 검증과 남은
  제한 사항을 기록하며 관련 이슈는 `Closes #<번호>` 형식으로 연결합니다.
- 제출 전 자체 검토를 수행하고 관련 검사 결과와 문서 및 테스트 변경 여부를
  확인합니다.
