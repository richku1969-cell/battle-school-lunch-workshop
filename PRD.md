# 급식 배틀 - 학교 급식 조회 앱 MCP 서버 PRD

## 1. 문서 개요

이 문서는 NEIS 공개 API를 활용해 학교별 중식 정보를 조회하는 Python 기반 MCP
서버의 제품 요구사항을 정의한다. 본 서버는 기존 웹용 백엔드 API와 별도 용도로
사용되며, MCP 클라이언트가 학교 검색과 급식 조회 도구를 사용할 수 있도록
제공한다. 구체적인 구현 방식은 `TRD.md`에서 정의한다.

## 2. 제품 배경과 목적

기존 웹 애플리케이션용 백엔드는 브라우저 기반 UI를 위한 HTTP API에 초점을 둔다.
이번 범위에서는 같은 NEIS 공개 데이터를 MCP 클라이언트에서도 활용할 수 있도록,
도구 기반 상호작용에 적합한 독립 실행형 MCP 서버를 제공한다. 사용자는 교육청
코드나 학교 코드를 직접 알지 않아도 학교를 찾고 기간별 중식 정보를 조회할 수
있어야 한다.

### 목표

- 기존 백엔드 API와 독립적으로 실행되는 Python 기반 MCP 서버를 제공한다.
- 학교명 일부를 이용한 학교 후보 검색 도구를 제공한다.
- 선택한 학교와 날짜 범위를 기준으로 중식 정보를 반환하는 조회 도구를 제공한다.
- 입력 오류, 빈 결과, 외부 서비스 오류, 지연 상황을 MCP 클라이언트가 구분할 수
  있게 전달한다.
- Docker Compose 환경에서 프론트엔드, 기존 백엔드와 함께 기동 가능한 개발·검증
  구성을 마련한다.

### 성공 기준

- MCP 클라이언트가 서버에 연결해 도구 목록을 조회할 수 있다.
- 사용자가 학교 검색 → 학교 식별 → 날짜 범위 입력 → 중식 결과 확인 흐름을 도구
  호출만으로 완료할 수 있다.
- 학교 검색 결과 없음, 급식 정보 없음, 입력 오류, NEIS 오류가 서로 다른 의미로
  처리된다.
- MCP 서버가 기존 프론트엔드 및 백엔드와 함께 Compose 환경에서 실행된다.

## 3. 대상 사용자

- Claude Desktop, GitHub Copilot, 기타 MCP 호환 클라이언트에서 급식 정보를 조회하려는 개발자 및 사용자
- 학교별 급식 데이터를 도구 호출로 활용하려는 워크숍 참가자

사용자는 NEIS의 교육청 코드, 학교 코드 또는 API 요청 형식을 직접 알 필요가
없다.

## 4. 제품 범위

### 포함 범위

- MCP Streamable HTTP 방식 서버 제공
- MCP 클라이언트 연결 및 도구 목록 조회 지원
- 학교명 일부를 이용한 학교 검색 도구
- 검색 결과에서 학교 식별에 필요한 교육청 코드, 학교 코드, 지역, 학교 종류 제공
- 선택한 학교와 시작일·종료일을 이용한 **중식** 조회 도구
- 날짜별 급식 메뉴 및 NEIS가 제공하는 부가 정보 반환
- 입력 오류, 빈 결과, 외부 서비스 오류, 응답 지연에 대한 MCP 표준 기반 오류 처리
- 기존 프론트엔드·백엔드와 함께 실행되는 Docker Compose 구성
- 도구 조회·호출과 NEIS 연동에 대한 단위 및 통합 테스트

### 제외 범위

- 브라우저 UI 또는 MCP 전용 시각화 화면 제공
- 조식 또는 석식 조회
- 로그인, 사용자 계정 및 즐겨찾기
- 급식 데이터의 장기 저장 및 캐싱 영속화
- 급식 정보의 생성, 수정 또는 삭제
- 영양 또는 알레르기 관련 의학적 판단

## 5. 핵심 사용자 흐름

### 1단계: MCP 서버 연결 및 도구 확인

1. MCP 클라이언트가 Streamable HTTP 방식으로 서버에 연결한다.
2. 클라이언트는 서버가 제공하는 도구 목록을 조회한다.
3. 사용자는 학교 검색 도구와 급식 조회 도구의 사용 가능 여부를 확인한다.

### 2단계: 학교 검색 및 선택

1. 사용자가 학교 이름의 일부를 입력해 학교 검색 도구를 호출한다.
2. 시스템은 일치하는 학교를 학교명, 교육청 코드, 학교 코드, 지역, 학교 종류
   등으로 구분해 반환한다.
3. 사용자가 조회할 학교 한 곳을 선택한다.

### 3단계: 날짜 범위 기반 중식 조회

1. 사용자가 선택한 학교 식별 정보와 시작일·종료일을 입력해 급식 조회 도구를
   호출한다.
2. 시스템은 입력값을 검증하고 선택한 학교와 기간의 중식 정보를 조회한다.
3. 시스템은 급식일 기준 오름차순으로 결과를 반환한다.
4. 각 결과에는 최소한 날짜와 메뉴를 포함하고, 제공되는 경우 열량·영양·원산지
   등 부가 정보를 함께 반환한다.

## 6. 기능 요구사항

| ID | 요구사항 | 인수 기준 |
|----|----------|-----------|
| FR-01 | MCP 서버는 Streamable HTTP 방식으로 실행되어야 한다. | MCP 클라이언트가 서버에 연결할 수 있고 세션 초기화 및 도구 목록 조회가 가능하다. |
| FR-02 | MCP 서버는 기존 웹 백엔드 API와 독립적으로 실행되어야 한다. | MCP 서버 프로세스가 기존 백엔드와 별도 엔트리포인트, 별도 포트 또는 별도 서비스로 기동된다. |
| FR-03 | 사용자는 학교명 일부로 후보 학교를 검색하는 MCP 도구를 사용할 수 있어야 한다. | 공백을 제외한 검색어가 있을 때 도구 호출이 성공하고 후보 학교 목록을 반환한다. |
| FR-04 | 학교 검색 결과는 학교 식별에 필요한 정보를 제공해야 한다. | 각 항목에 학교명, 교육청 코드, 학교 코드와 함께 지역 및 학교 종류 등 식별 정보가 포함된다. |
| FR-05 | 사용자는 선택한 학교와 날짜 범위로 중식 정보를 조회하는 MCP 도구를 사용할 수 있어야 한다. | 유효한 학교 식별 정보와 날짜 범위를 전달하면 기간 내 중식 결과가 날짜순으로 반환된다. |
| FR-06 | 시스템은 중식만 조회해야 한다. | NEIS 조회 시 중식 조건을 적용하고 다른 식사 구분의 결과를 반환하지 않는다. |
| FR-07 | 시스템은 날짜별 급식 메뉴와 제공 가능한 부가 정보를 반환해야 한다. | 각 결과에 날짜와 메뉴가 포함되며, NEIS가 제공한 경우 식사 구분, 열량, 영양 정보, 원산지 정보도 함께 반환된다. |
| FR-08 | 시스템은 도구 수준에서 명확한 오류를 반환해야 한다. | 입력 오류, 학교 검색 결과 없음, 급식 정보 없음, 외부 API 오류 및 지연이 민감 정보 없이 구분되어 표현된다. |
| FR-09 | MCP 서버는 Docker Compose 구성에 포함되어야 한다. | 기존 프론트엔드 및 백엔드와 함께 Compose 명령으로 기동할 수 있다. |
| FR-10 | 시스템은 도구 조회·호출과 NEIS 연동에 대한 테스트를 제공해야 한다. | 단위 및 통합 테스트가 존재하고 핵심 정상·실패 흐름을 검증한다. |

## 7. 예외 및 상태 요구사항

| 상황 | 기대 동작 |
|------|-----------|
| 학교 검색어가 비어 있음 | 학교 검색 도구는 호출 실패로 처리하고 검색어 입력 오류를 반환한다. |
| 학교 검색 결과가 없음 | 시스템 장애로 표현하지 않고 일치하는 학교가 없음을 나타내는 빈 결과 또는 명시적 메시지를 반환한다. |
| 학교 식별 정보가 누락됨 | 급식 조회 도구는 호출 실패로 처리하고 필수 입력 누락 오류를 반환한다. |
| 날짜가 누락되거나 형식이 잘못됨 | 급식 조회 도구는 호출 실패로 처리하고 날짜 입력 오류를 반환한다. |
| 시작일이 종료일보다 늦음 | 급식 조회 도구는 호출 실패로 처리하고 올바른 날짜 범위를 안내한다. |
| 날짜 범위가 최대 허용 기간을 초과함 | 급식 조회 도구는 호출 실패로 처리하고 허용 기간 이내로 좁히도록 안내한다. |
| 기간 내 중식 정보가 없음 | 오류 대신 선택한 조건에 급식 정보가 없음을 나타내는 빈 결과를 반환한다. |
| NEIS 인증 또는 요청 제한 오류 | MCP 오류 응답 또는 구조화된 실패 결과로 현재 조회할 수 없음을 전달하되 비밀 정보는 노출하지 않는다. |
| 네트워크 또는 서버 지연/오류 | 재시도 가능 여부를 구분할 수 있는 오류를 반환하고 무한 대기를 만들지 않는다. |
| 일부 응답 필드가 없음 | 존재하는 정보만 반환하며 필수 식별 정보가 없는 항목은 성공 데이터처럼 만들지 않는다. |

## 8. 사용자 경험 요구사항

- MCP 클라이언트는 도구 이름과 설명만으로 학교 검색과 급식 조회 용도를 이해할 수
  있어야 한다.
- 학교 검색 도구의 반환값만으로 사용자가 조회 대상 학교를 식별할 수 있어야 한다.
- 급식 조회 도구의 반환값은 클라이언트가 날짜별 결과를 그대로 표시하거나 후속
  처리할 수 있도록 구조화되어야 한다.
- 오류 응답은 민감 정보 없이 사용자 또는 호출자가 다음 행동을 결정할 수 있을
  정도로 명확해야 한다.
- 날짜는 `Asia/Seoul` 기준의 캘린더 날짜로 다루며 시작일과 종료일을 모두 포함하는
  범위로 해석한다.

## 9. 비기능 요구사항

### 보안과 개인정보

- NEIS API 키는 MCP 클라이언트 또는 사용자에게 노출하지 않는다.
- 애플리케이션은 사용자 개인정보를 요구하거나 저장하지 않는다.
- 오류 메시지와 로그에 API 키 또는 민감한 환경 설정을 포함하지 않는다.

### 성능과 신뢰성

- MCP 도구 호출은 상위 API 지연 중에도 무한 대기 상태로 남지 않아야 한다.
- 외부 API의 빈 결과와 오류 응답을 구분한다.
- 성능 목표와 상위 API 타임아웃·재시도 정책은 `TRD.md`에서 정의한다.

### 호환성과 운영성

- 주요 MCP 호환 클라이언트가 지원하는 Streamable HTTP 연결 방식과 호환되어야
  한다.
- Docker Compose 기반 개발 환경에서 프론트엔드, 기존 백엔드, MCP 서버를 함께
  실행할 수 있어야 한다.

## 10. 데이터 표시 및 반환 원칙

- 급식 정보는 NEIS가 제공한 데이터를 사실 그대로 기반으로 한다.
- 메뉴 문자열의 줄 구분과 알레르기 표기 등 원본 의미를 변경하지 않는 범위에서
  구조화한다.
- 부가 정보가 없는 경우 값을 추정하지 않는다.
- 급식 정보는 참고용 데이터이며 전문적인 영양 또는 의료 조언이 아님을 전제로
  한다.

## 11. 완료 조건

- MCP 클라이언트가 서버에 연결해 도구 목록을 조회할 수 있다.
- 학교 검색 도구와 중식 조회 도구가 `data/openapi.json` 기반으로 동작한다.
- 정상 경로, 검색 결과 없음, 잘못된 입력, 급식 정보 없음, 외부 API 오류 및 지연이
  구분되어 처리된다.
- MCP 서버가 Docker Compose 구성에 포함되어 기존 프론트엔드 및 백엔드와 함께
  실행된다.
- 단위 및 통합 테스트가 도구 조회·호출과 NEIS 연동 핵심 흐름을 검증한다.
- 상세 기술 설계와 테스트 전략은 `TRD.md`와 일치해야 한다.
