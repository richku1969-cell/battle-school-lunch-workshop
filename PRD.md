# 급식 배틀 - 학교 급식 조회 앱 PRD

## 1. 문서 개요

이 문서는 NEIS 공개 API를 활용해 학교별 중식 정보를 조회하는 웹
애플리케이션의 제품 요구사항을 정의한다. 제품 범위와 사용자 경험에 대한 단일
기준으로 사용하며, 구체적인 구현 방식은 `TRD.md`에서 정의한다.

## 2. 제품 배경과 목적

학교 급식 정보는 공개되어 있지만 사용자가 학교 식별 코드나 교육청 코드를 직접
알아야 하면 접근하기 어렵다. 이 제품은 학교 이름 일부만 입력해 원하는 학교를
찾고, 선택한 기간의 중식 메뉴를 날짜별로 쉽게 확인할 수 있게 한다.

### 목표

- 학교명 일부를 이용한 간편한 학교 검색을 제공한다.
- 학교와 날짜 범위를 기준으로 중식 정보를 일관된 형식으로 보여준다.
- 로딩, 빈 결과, 잘못된 입력, 외부 서비스 오류를 사용자가 이해할 수 있게
  안내한다.
- 향후 급식 비교와 분석 기능이 활용할 수 있는 조회 기반을 마련한다.

### 성공 기준

- 사용자가 학교 검색, 학교 선택, 날짜 범위 선택, 결과 확인을 한 흐름에서
  완료할 수 있다.
- 조회된 중식은 날짜 순으로 구분되어 학교명, 급식일, 메뉴를 식별할 수 있다.
- 유효하지 않은 입력과 데이터가 없는 상태가 시스템 장애와 구분되어 표시된다.

## 3. 대상 사용자

- 본인 또는 가족이 다니는 초·중·고등학교의 급식을 확인하려는 학생과 보호자
- 학교별 급식 정보를 확인하려는 교직원 및 일반 사용자

사용자는 NEIS의 교육청 코드, 학교 코드 또는 API 사용법을 알 필요가 없다.

## 4. 제품 범위

### 포함 범위

- 학교명 일부를 이용한 학교 검색
- 검색 결과에서 학교 식별에 필요한 지역, 학교 종류 등의 정보 표시
- 학교 한 곳 선택
- 시작일과 종료일을 포함하는 날짜 범위 선택
- 선택한 학교와 기간의 **중식** 조회
- 날짜별 급식 메뉴 및 NEIS가 제공하는 부가 정보 표시
- 열량과 영양소처럼 수치와 단위가 확인되는 급식 정보의 차트 표시
- 각 단계의 로딩, 빈 상태, 입력 오류 및 조회 오류 안내
- 데스크톱과 모바일 웹에서 사용할 수 있는 반응형 UI

### 제외 범위

- 조식 또는 석식 조회
- 로그인, 사용자 계정 및 즐겨찾기
- 급식 정보의 생성, 수정 또는 삭제
- 급식 데이터의 장기 저장 및 NEIS 원본 데이터 보정
- 영양 또는 알레르기 관련 의학적 판단

## 5. 핵심 사용자 흐름

### 1단계: 학교 검색 및 선택

1. 사용자가 학교 이름의 일부를 입력한다.
2. 사용자가 검색을 실행한다.
3. 시스템은 일치하는 학교를 학교명, 지역, 학교 종류 등으로 구분해 보여준다.
4. 사용자가 조회할 학교 한 곳을 선택한다.

### 2단계: 날짜 범위 선택

1. 시스템은 선택한 학교를 명확히 표시한다.
2. 사용자가 조회 시작일과 종료일을 선택한다.
3. 시스템은 시작일이 종료일보다 늦지 않은지 검증한다.
4. 사용자가 급식 조회를 실행한다.

### 3단계: 날짜별 중식 결과 표시

1. 시스템은 선택한 학교와 날짜 범위의 중식 정보를 조회한다.
2. 시스템은 급식일 기준 오름차순으로 결과를 구분해 표시한다.
3. 각 결과에는 최소한 날짜와 메뉴를 표시한다.
4. NEIS가 제공한 경우 식사 구분, 열량, 영양 정보, 원산지 정보도 함께
   표시할 수 있다.
5. 열량과 영양소처럼 수치와 단위가 확인되는 정보는 원본 값도 함께 제공하면서
   날짜별 또는 항목별 차트로 시각화한다.
6. 사용자는 학교 또는 날짜 범위를 변경해 다시 조회할 수 있다.

## 6. 기능 요구사항

| ID | 요구사항 | 인수 기준 |
|----|----------|-----------|
| FR-01 | 사용자는 학교명 일부로 학교를 검색할 수 있어야 한다. | 공백을 제외한 검색어가 있을 때 검색을 실행하고 일치하는 학교 목록을 표시한다. |
| FR-02 | 검색 결과는 동명이거나 유사한 학교를 구분할 정보를 제공해야 한다. | 각 항목에 학교명과 함께 지역 및 학교 종류 등 NEIS가 제공하는 식별 정보를 표시한다. |
| FR-03 | 사용자는 검색 결과에서 학교 한 곳을 선택할 수 있어야 한다. | 선택된 학교가 시각적으로 구분되고 다음 단계에서 학교명으로 확인된다. |
| FR-04 | 사용자는 시작일과 종료일을 선택할 수 있어야 한다. | 두 날짜가 모두 입력되고 시작일이 종료일과 같거나 앞선 경우에만 조회할 수 있다. |
| FR-05 | 시스템은 선택한 학교의 중식만 조회해야 한다. | 백엔드 조회에서 중식 조건을 적용하고 다른 식사 구분의 결과를 표시하지 않는다. |
| FR-06 | 시스템은 기간 내 급식을 날짜별로 표시해야 한다. | 결과를 급식일 오름차순으로 표시하며 각 항목에 날짜와 메뉴가 포함된다. |
| FR-07 | 사용자는 진행 상태를 알 수 있어야 한다. | 학교 및 급식 조회 중 로딩 상태를 표시하고 중복 요청을 방지한다. |
| FR-08 | 사용자는 조건을 수정해 재조회할 수 있어야 한다. | 학교 또는 날짜 범위를 변경한 뒤 새 조건의 결과를 조회할 수 있다. |
| FR-09 | 시스템은 수치형 급식 정보를 차트로 보여줘야 한다. | NEIS가 열량 또는 영양소의 수치와 단위를 제공하면 원본 수치 표시와 함께 비교 가능한 값만 차트로 표시한다. |

## 7. 예외 및 상태 요구사항

| 상황 | 기대 동작 |
|------|-----------|
| 학교 검색어가 비어 있음 | 검색을 요청하지 않고 검색어 입력 안내를 표시한다. |
| 학교 검색 결과가 없음 | 장애로 표현하지 않고 일치하는 학교가 없음을 알리며 다른 검색어를 제안한다. |
| 학교 검색 결과가 너무 많음 | 표시 가능한 결과와 함께 검색어를 더 구체적으로 입력하라는 안내를 표시하며 결과가 전체인 것처럼 오해하게 하지 않는다. |
| 학교를 선택하지 않음 | 날짜별 급식 조회를 허용하지 않고 학교 선택 안내를 표시한다. |
| 날짜가 누락됨 | 조회를 허용하지 않고 시작일과 종료일 입력 안내를 표시한다. |
| 시작일이 종료일보다 늦음 | 조회를 허용하지 않고 올바른 날짜 범위를 안내한다. |
| 기간 내 중식 정보가 없음 | 오류 화면 대신 선택한 조건에 급식 정보가 없음을 표시한다. |
| NEIS 인증 또는 요청 제한 오류 | 비밀 정보나 내부 응답을 노출하지 않고 현재 조회할 수 없다는 안내를 표시한다. |
| 네트워크 또는 서버 오류 | 재시도 가능한 오류임을 알리고 사용자가 같은 조건으로 다시 요청할 수 있게 한다. |
| 일부 응답 필드가 없음 | 존재하는 정보만 표시하며 필수 정보가 없는 잘못된 항목은 성공 결과처럼 만들지 않는다. |

## 8. 사용자 경험 요구사항

- 3단계의 현재 위치와 완료 여부를 명확하게 보여준다.
- 학교를 변경하면 이전 학교의 급식 결과를 제거하거나 새 조회 전 상태로
  전환하여 서로 다른 학교의 데이터가 섞이지 않게 한다.
- 날짜와 상태 메시지는 한국어 사용자가 이해하기 쉬운 형식으로 표시한다.
- 키보드만으로 검색, 학교 선택, 날짜 입력, 조회를 수행할 수 있어야 한다.
- 입력 요소에는 연결된 레이블을 제공하고 로딩 및 오류 상태는 색상만으로
  전달하지 않는다.
- 차트의 값과 의미를 색상에만 의존하지 않도록 축, 단위, 범례 또는 데이터
  레이블을 제공하고 동일한 정보를 텍스트로도 확인할 수 있게 한다.
- 모바일 화면에서도 핵심 흐름과 결과를 가로 스크롤 없이 사용할 수 있어야
  한다.

## 9. 비기능 요구사항

### 보안과 개인정보

- NEIS API 키는 브라우저 또는 사용자에게 노출하지 않는다.
- 애플리케이션은 사용자 개인정보를 요구하거나 저장하지 않는다.
- 오류 메시지와 로그에 API 키 또는 민감한 환경 설정을 포함하지 않는다.

### 성능과 신뢰성

- 동일 단계에서 사용자가 반복 클릭해 중복 요청을 만들지 않게 한다.
- 외부 API 지연 중에는 진행 상태를 표시하고 응답 실패를 무한 로딩으로
  남기지 않는다.
- 외부 API의 빈 결과와 오류 응답을 구분한다.

### 호환성과 접근성

- 최신 주요 데스크톱 및 모바일 브라우저에서 핵심 흐름이 동작해야 한다.
- 핵심 인터랙션은 WCAG 2.1 AA 수준의 기본 원칙을 고려한다.

## 10. 데이터 표시 원칙

- 급식 정보는 NEIS가 제공한 데이터를 사실 그대로 기반으로 한다.
- 메뉴 문자열의 줄 구분과 알레르기 표기 등 원본 형식을 사용자가 읽기 쉬운
  형태로 표현할 수 있으나 원본의 의미를 변경하지 않는다.
- 차트는 수치와 단위를 모두 확인할 수 있는 값만 사용하고 서로 다른 단위의 값을
  같은 척도에서 직접 비교하지 않는다.
- 차트에 사용한 원본 수치와 단위를 텍스트로도 제공하며 값이 없거나 숫자로
  해석할 수 없으면 추정하지 않고 해당 항목을 차트에서 제외한다.
- 부가 정보가 없는 경우 값을 추정하지 않는다.
- 급식 정보가 전문적인 영양 또는 의료 조언이 아님을 전제로 한다.

## 11. 완료 조건

- 3단계 핵심 흐름과 본 문서의 기능 및 예외 요구사항을 구현한다.
- 프론트엔드는 NEIS API를 직접 호출하지 않고 백엔드를 통해서만 데이터를
  조회한다.
- 정상 경로, 검색 결과 없음, 잘못된 날짜 범위, 급식 정보 없음 및 외부 API
  오류가 각각 구분되는지 검증한다.
- 수치와 단위가 확인되는 급식 정보가 원본 값과 일치하는 차트로 표시되는지
  검증한다.
- 상세 기술 설계와 테스트 전략은 `TRD.md`와 일치해야 한다.
